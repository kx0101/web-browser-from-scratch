[package]
name = "web-browser"
version = "0.0.1"
edition = "2024"

[[bin]]
name = "web-browser"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
image = { version = "0.25", default-features = false, features = ["png"] }
